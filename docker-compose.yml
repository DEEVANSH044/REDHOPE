version: "3.9"

services:
  app:
    build: .
    container_name: redhope-nextjs
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - postgres
      - redis
    networks:
      - redhope_net

  postgres:
    image: postgres:15-alpine
    container_name: redhope-postgres
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - redhope_net

  redis:
    image: redis:7-alpine
    container_name: redhope-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - redhope_net

networks:
  redhope_net:
    driver: bridge

volumes:
  postgres_data:
