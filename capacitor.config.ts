import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 's84-0126-triad-redhope',
  webDir: 'public'
};

export default config;
